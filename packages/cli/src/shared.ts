export { isSharedLibrary as isShared, SHARED_LIBRARIES as SHARED_PREFIXES } from '@platform/sdk'

export const FORBIDDEN = ['zone.js', '@microsoft/signalr', '@module-federation/runtime', '@softarc/native-federation'] as const
