/**
 * §9.2 item 7: `mfe types` writes `platform-registry.d.ts` augmenting this interface.
 * Without augmentation everything works, untyped.
 */
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface Register {}
