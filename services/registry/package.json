{
  "name": "@platform/registry",
  "version": "0.1.0",
  "description": "MFE registry: published manifests, live versions, the release list, and a dev artifact store.",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "tsx src/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@hono/node-server": "^1",
    "@platform/sdk": "workspace:*",
    "hono": "^4",
    "tsx": "^4"
  }
}
